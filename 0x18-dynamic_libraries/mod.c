#include "main.h"

/**
 * mod - finds the modulo of two integers
 * @a: takes in a number
 * @b: takes in a number
 * Return: modulo of the two numbers
 */
int mod(int a, int b)
{
    return (a % b);
}
