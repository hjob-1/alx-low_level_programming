#include "main.h"
/**
 * add - add two integer number and return thier result
 * @a: number being added
 * @b: number being added
 * Return: it returns the summation of the two numbers
 */
int add(int a, int b)
{
	return (a + b);
}
